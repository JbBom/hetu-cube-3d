# v1.0.3

This release polishes the project presentation: adds screenshots, improves README, and enhances SEO/social sharing metadata.

## Changes

- Added hero and full-page screenshots to `docs/assets/` and embedded them in README.
- Rewrote README with emoji section headers, feature overview table, and "Try Live" badge.
- Added GitHub stars badge to README header.
- Added Open Graph and Twitter Card meta tags to `index.html` and `hetu-cube.html`.
- Added Schema.org `WebApplication` structured data to `index.html`.
- Added `robots.txt`, `sitemap.xml`, and `CONTRIBUTING.md`.
- Updated `.gitignore` with additional patterns.

## Notes

- Screenshots are derived from the live GitHub Pages demo.
- OG image URLs point to `docs/assets/screenshot-hero.png` via the Pages CDN.
- The iOS `WKWebView` shell is unaffected; it uses local assets only.
